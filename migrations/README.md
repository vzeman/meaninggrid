# Migrations

MeaningGrid will use Alembic for Postgres schema migrations.

The first migration sequence is specified in:

```text
docs/23-initial-database-schema-v0.md
```

This folder is intentionally a placeholder until the database implementation
starts.
